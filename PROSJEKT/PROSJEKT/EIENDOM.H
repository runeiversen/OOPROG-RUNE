#if !defined(__EIENDOM_H)
#define __EIENDOM_H

#include "LISTTOOL2.H"
#include "ENUM.H"

class Eiendom : Num_element{
private:
   int eiendomNr, dato, bruksNr, ansatt, pris, areal;
   char* gateAdr, * postAdr, * eierNavn, * kommune, * beskrivelse;
   enum type;

public:

};

#endif